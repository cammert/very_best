<% was_validated = dish.errors.any? %>

<%= form_with(model: dish, local: true, novalidate: true, class: "mb-3") do |f| %>
  <!-- Label and input for dish start -->
  <div class="form-group">
    <% dish_class = "form-control" %>
    <% dish_was_invalid = dish.errors.include?(:dish) %>

    <% if was_validated %>
      <% if dish_was_invalid %>
        <% dish_class << " is-invalid" %>
      <% else %>
        <% dish_class << " is-valid" %>
      <% end %>
    <% end %>

    <%= f.label :dish %>
    <!-- Input for dish start -->
    <%= f.text_field :dish, class: dish_class, placeholder: "Enter dish" %>
    <!-- Input for dish end -->
    <% if dish_was_invalid %>
      <% dish.errors.full_messages_for(:dish).each do |message| %>
        <div class="invalid-feedback d-flex">
          <%= message %>
        </div>
      <% end %>
    <% end %>
  </div>
  <!-- Label and input for dish end -->
  <!-- Label and input for venue_id start -->
  <div class="form-group">
    <% venue_class = "form-control" %>
    <% venue_was_invalid = dish.errors.include?(:venue) %>

    <% if was_validated %>
      <% if venue_was_invalid %>
        <% venue_class << " is-invalid" %>
      <% else %>
        <% venue_class << " is-valid" %>
      <% end %>
    <% end %>

    <%= f.label :venue_id %>
    <!-- Input for venue_id start -->
    <%= f.collection_select :venue_id, Venue.all, :id, :to_s, {}, class: "form-control" %>
    <!-- Input for venue_id end -->
    <% if venue_was_invalid %>
      <% dish.errors.full_messages_for(:venue).each do |message| %>
        <div class="invalid-feedback d-flex">
          <%= message %>
        </div>
      <% end %>
    <% end %>
  </div>
  <!-- Label and input for venue_id end -->
  <!-- Label and input for dish_type start -->
  <div class="form-group">
    <% dish_type_class = "form-control" %>
    <% dish_type_was_invalid = dish.errors.include?(:dish_type) %>

    <% if was_validated %>
      <% if dish_type_was_invalid %>
        <% dish_type_class << " is-invalid" %>
      <% else %>
        <% dish_type_class << " is-valid" %>
      <% end %>
    <% end %>

    <%= f.label :dish_type %>
    <!-- Input for dish_type start -->
    <%= f.text_field :dish_type, class: dish_type_class, placeholder: "Enter dish type" %>
    <!-- Input for dish_type end -->
    <% if dish_type_was_invalid %>
      <% dish.errors.full_messages_for(:dish_type).each do |message| %>
        <div class="invalid-feedback d-flex">
          <%= message %>
        </div>
      <% end %>
    <% end %>
  </div>
  <!-- Label and input for dish_type end -->
  <%= f.button class: "btn btn-outline-secondary btn-block" %>
<% end %>
